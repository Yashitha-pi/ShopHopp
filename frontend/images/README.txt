Product images are loaded from external URLs (picsum.photos placeholders) set in
backend/database.py's seed data, so this folder is empty by default.

Drop any local image assets you want to reference from the frontend here, e.g.:
  <img src="images/logo.png" />
